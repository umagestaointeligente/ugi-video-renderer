CORREÇÃO CIRÚRGICA — UGI REEL RENDERER R43.7.12
FFMPEG PREINSTALLED FAST PATH
PRESERVAR TODO O PIPELINE EXISTENTE

OBJETIVO

Corrigir exclusivamente o bootstrap de FFmpeg do workflow render-video.yml.

O workflow R43.7.11 falhou antes do render porque entrou no step:

Controlled apt recovery for missing system tools

e tentou instalar via apt aproximadamente 87 pacotes / 62,8 MB.

A execução terminou com:

Process completed with exit code 124

O render propriamente dito NÃO chegou a iniciar.

IMPORTANTE:

O step anterior:

Verify runner media tools

foi concluído com sucesso.

Portanto, não queremos simplesmente aumentar novamente o timeout.
Queremos eliminar instalação apt desnecessária.

==================================================
REGRA OBRIGATÓRIA R43.7.12
==================================================

1. Detectar primeiro, explicitamente:

command -v ffmpeg
command -v ffprobe
command -v curl

2. Executar:

ffmpeg -version
ffprobe -version
curl --version

3. Se ffmpeg + ffprobe + curl estiverem disponíveis:

- NÃO executar apt-get update;
- NÃO executar apt-get install;
- NÃO executar qualquer recuperação de pacotes;
- seguir imediatamente para o restante do pipeline.

4. Somente se alguma ferramenta obrigatória realmente estiver ausente:

executar recuperação mínima e controlada.

5. É PROIBIDO:

- apt upgrade;
- dist-upgrade;
- instalação indiscriminada de dependências;
- reinstalar FFmpeg quando ffmpeg e ffprobe já existirem;
- modificar componentes não relacionados ao bootstrap.

6. Se FFmpeg estiver ausente e a recuperação mínima não puder ser concluída rapidamente:

falhar explicitamente com diagnóstico claro.

NÃO ficar aguardando apt indefinidamente.

==================================================
PRESERVAÇÃO ABSOLUTA
==================================================

Não alterar a lógica existente de:

- workflow_dispatch;
- inputs;
- metadata pass;
- draftId;
- approvalDraftId;
- existingDraftReused;
- renderId;
- content_id;
- Pexels;
- Kokoro PT-BR;
- TTS;
- música;
- composição audiovisual;
- FFmpeg render;
- masters independentes;
- Instagram;
- TikTok;
- YouTube;
- validação MP4;
- R2;
- upload;
- callback para Lola;
- Central de Aprovação;
- tratamento de sucesso/falha.

Não alterar secrets.

Não alterar Worker.

Não alterar Schema.

Não criar outro workflow.

Modificar exclusivamente render-video.yml.

==================================================
VERSIONAMENTO
==================================================

Atualizar identificação para:

UGI Reel Renderer R43.7.12
FFmpeg Preinstalled Fast Path + Metadata Pass

==================================================
CRITÉRIO DE SUCESSO
==================================================

Em runner que já possui FFmpeg:

Verify runner media tools
→ identifica ffmpeg/ffprobe/curl
→ confirma versões
→ FAST PATH

Controlled apt recovery
→ SKIPPED

Pipeline segue diretamente para:

Kokoro / dependências necessárias
→ cache/modelos
→ render mode
→ mídia
→ render
→ validação
→ R2
→ callback Lola

Não execute manualmente o workflow após editar.

Ao terminar:

1. salvar render-video.yml;
2. confirmar que R43.7.12 está salvo;
3. informar exatamente quais linhas/blocos foram modificados;
4. informar se o fast path está ativo;
5. NÃO disparar workflow manual.

HARD STOP APÓS SALVAR.
