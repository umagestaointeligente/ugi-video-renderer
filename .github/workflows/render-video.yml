R42 — PATCH CONTROLADO DO render-video.yml

OBJETIVO
Preservar o workflow R41.1 que já funciona e permitir ao renderer R42 gerar
três masters específicos: Instagram, TikTok e YouTube Shorts.

NÃO ALTERAR:
- workflow_dispatch
- PEXELS_API_KEY
- Kokoro
- aquisição Pexels
- UGI_VIDEO_UPLOAD_URL
- UGI_VIDEO_UPLOAD_KEY
- /api/video-upload
- /api/video-status
- Worker
- OpenAPI
- Central de Aprovação

ALTERAÇÕES:

1) O passo "Render UGI Reel R41" pode ser renomeado para:
   Render UGI Multi-Platform R42

   O comando continua:
   python render-reel.py

2) A aquisição Pexels continua salvando:
   output/media/scene-1.mp4
   ...
   output/media/scene-6.mp4

   Não é necessário baixar 18 vídeos nesta fase.
   Os mesmos seis ativos-base são remontados de forma diferente pelo R42.

3) Em "Validate R41 Reel", substituir/adicionar validações para:
   output/ugi-reel.mp4
   output/instagram-reel.mp4
   output/tiktok-reel.mp4
   output/youtube-short.mp4
   output/r42-platform-manifest.json
   output/r42-storyboard-instagram.json
   output/r42-storyboard-tiktok.json
   output/r42-storyboard-youtube.json

4) Tolerâncias esperadas:
   Instagram: 32 s ± 0,30
   TikTok: 24 s ± 0,30
   YouTube Shorts: 36 s ± 0,30

5) Em "Preserve GitHub artifact", incluir:
   output/instagram-reel.mp4
   output/tiktok-reel.mp4
   output/youtube-short.mp4
   output/ugi-reel.mp4
   output/r42-platform-manifest.json
   output/r42-storyboard-instagram.json
   output/r42-storyboard-tiktok.json
   output/r42-storyboard-youtube.json
   output/pexels-attribution.json
   output/casting-policy.json

6) ENTREGA R2 — COMPATIBILIDADE ATUAL
   NÃO altere o passo atual de upload.
   Ele continuará enviando:
   output/ugi-reel.mp4

   No R42, output/ugi-reel.mp4 é uma cópia da versão Instagram.

   Motivo:
   o Worker atual ainda trabalha com um videoUrl por renderId.
   Assim validamos o motor multi-plataforma sem quebrar Central/R2.

7) PRÓXIMA CAMADA DEPOIS DA VALIDAÇÃO R42
   Atualizar Worker + OpenAPI + Central para:
   assets.instagram
   assets.tiktok
   assets.youtube

   Cada asset terá:
   platform
   videoUrl
   videoKey
   duration
   content_id
   experiment_id
   variant
   status
   approvalStatus

REGRA:
Primeiro validar os 3 arquivos no GitHub Artifact.
Só depois alterar Worker/OpenAPI/Central.

