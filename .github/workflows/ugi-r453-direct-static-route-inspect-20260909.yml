name: UGI R45.3 Direct Static Route Inspect 2026-09-09
on:
  push:
    paths:
      - '.github/workflows/ugi-r453-direct-static-route-inspect-20260909.yml'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.UGI_ADMIN_CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.UGI_ADMIN_CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - name: Redacted static publish contract readback
        shell: bash
        run: |
          set -euo pipefail
          echo "::add-mask::$CLOUDFLARE_API_TOKEN"
          curl --fail --silent --show-error -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/lola-operacional-ugi" -o /tmp/w
          python - <<'PY'
          import re
          s=open('/tmp/w','rb').read().decode('utf-8','ignore')
          print('LSI_STATIC_PUBLISH_CONTRACT_V1')
          for needle in ['/api/r45/static-publish','function createBufferStatic','staticPublish','handleStaticPublish','function bufferCreatePost','async function bufferCreatePost']:
            pos=0; seen=0
            while seen<5:
              i=s.find(needle,pos)
              if i<0: break
              clean=re.sub(r'\s+',' ',s[max(0,i-500):i+2600])
              print('SNIP='+clean[:3100])
              pos=i+len(needle); seen+=1
          PY
# static-contract-readback 2026-09-09T21:29:00-03:00
