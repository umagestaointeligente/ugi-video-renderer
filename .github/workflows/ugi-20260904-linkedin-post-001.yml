name: UGI LinkedIn Editorial 001 — retired launcher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration
        run: |
          echo 'RETIRED=true'
          echo 'The launch-day 15-minute Buffer polling loop is disabled.'
          echo 'LinkedIn publishing now runs through the canonical rate-limit-aware adapter.'
          echo 'Queue: control-plane/publisher-hub/linkedin-queue/'
          echo 'Workflow: .github/workflows/ugi-linkedin-publisher.yml'
