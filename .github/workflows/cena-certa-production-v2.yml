name: Cena Certa V2 Production

on:
  push:
    branches: [feat/cena-certa-factory-v2-certified]
    paths:
      - 'ops/cena-certa/dispatch.json'
  workflow_dispatch:

permissions:
  contents: write
  actions: read
  packages: read

env:
  ROOT: vendor/cena-certa-factory-v2/src
  RUNTIME_DIGEST: sha256:5e35dcb15a263b190fd91ad759c6e570d16a186c180fe18fe3f8080ac86acf9c

concurrency:
  group: cena-certa-production-v2
  cancel-in-progress: false

jobs:
  dispatch:
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    outputs:
      enabled: ${{ steps.read.outputs.enabled }}
      mode: ${{ steps.read.outputs.mode }}
      batch_path: ${{ steps.read.outputs.batch_path }}
      batch_sha256: ${{ steps.read.outputs.batch_sha256 }}
      request_id: ${{ steps.read.outputs.request_id }}
      deadline_epoch: ${{ steps.read.outputs.deadline_epoch }}
      candidate_count: ${{ steps.read.outputs.candidate_count }}
      selected_count: ${{ steps.read.outputs.selected_count }}
      reserve_count: ${{ steps.read.outputs.reserve_count }}
      index_matrix: ${{ steps.read.outputs.index_matrix }}
    container:
      image: ghcr.io/umagestaointeligente/cena-certa-runtime@sha256:5e35dcb15a263b190fd91ad759c6e570d16a186c180fe18fe3f8080ac86acf9c
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v7
      - name: Compile and run zero-network proactive doctor
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile ops/cena-certa/r2_stage.py ops/cena-certa/route_guard.py ops/cena-certa/factory_doctor.py ops/cena-certa/dispatch_activate.py
          python ops/cena-certa/factory_doctor.py
      - id: read
        name: Validate single immutable dispatch and 12-minute deadline
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import datetime,hashlib,json,os,pathlib,re
          p=pathlib.Path('ops/cena-certa/dispatch.json')
          d=json.loads(p.read_text(encoding='utf-8'))
          if d.get('schema')!='CENA_CERTA_PRODUCTION_DISPATCH_V1':
              raise SystemExit('DISPATCH_SCHEMA_FAIL')
          enabled=bool(d.get('enabled'))
          mode=str(d.get('mode') or 'IDLE').upper()
          deadline=''; request_id=''; candidate_count=0; selected_count=0; reserve_count=0; index_matrix='[]'
          if not enabled:
              mode='IDLE'; batch_path=''; batch_sha=''
          else:
              if mode!='PREPARE':
                  raise SystemExit('DISPATCH_MODE_FAIL_ONLY_PREPARE_IS_CANONICAL')
              if str(d.get('prepared_run_id') or ''):
                  raise SystemExit('DISPATCH_PREPARED_RUN_FORBIDDEN_SINGLE_RUN')
              request_id=str(d.get('request_id') or '')
              candidate_count=int(d.get('candidate_count') or 10)
              selected_count=int(d.get('selected_count') or 8)
              reserve_count=int(d.get('reserve_count') if d.get('reserve_count') is not None else 2)
              if candidate_count<1 or candidate_count>10 or selected_count<1 or reserve_count<0 or candidate_count!=selected_count+reserve_count:
                  raise SystemExit('DISPATCH_COUNT_BINDING_FAIL')
              index_matrix=json.dumps(list(range(candidate_count)),separators=(',',':'))
              if not re.fullmatch(r'[A-Za-z0-9._-]{12,96}',request_id):
                  raise SystemExit('DISPATCH_REQUEST_ID_FAIL')
              batch_path=str(d.get('batch_path') or '')
              if not batch_path.startswith('ops/cena-certa/batches/') or not batch_path.endswith('.json'):
                  raise SystemExit('DISPATCH_BATCH_PATH_FAIL')
              bp=pathlib.Path(batch_path)
              if not bp.is_file():
                  raise SystemExit('DISPATCH_BATCH_MISSING')
              batch_sha=hashlib.sha256(bp.read_bytes()).hexdigest()
              if batch_sha!=str(d.get('batch_sha256') or ''):
                  raise SystemExit('DISPATCH_BATCH_SHA_FAIL')
              req=str(d.get('requested_at') or '')
              dt=datetime.datetime.fromisoformat(req.replace('Z','+00:00'))
              if dt.tzinfo is None:
                  raise SystemExit('DISPATCH_TIMEZONE_FAIL')
              now=datetime.datetime.now(datetime.timezone.utc)
              age=(now-dt.astimezone(datetime.timezone.utc)).total_seconds()
              if age < -60 or age > 720:
                  raise SystemExit(f'DISPATCH_SLA_WINDOW_FAIL seconds={age:.0f}')
              deadline=str(int(dt.timestamp())+720)
              if int(now.timestamp())>=int(deadline):
                  raise SystemExit('DISPATCH_DEADLINE_EXPIRED')
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as f:
              f.write(f'enabled={str(enabled).lower()}\nmode={mode}\nbatch_path={batch_path}\nbatch_sha256={batch_sha}\nrequest_id={request_id}\ndeadline_epoch={deadline}\ncandidate_count={candidate_count}\nselected_count={selected_count}\nreserve_count={reserve_count}\nindex_matrix={index_matrix}\n')
          print('DISPATCH_PASS',enabled,mode,batch_path,batch_sha,request_id,'deadline',deadline)
          PY
      - name: Validate exact dispatch-bound candidate batch
        if: steps.read.outputs.enabled == 'true'
        shell: bash
        run: |
          set -euo pipefail
          export PYTHONPATH="$ROOT"
          python -m factory.cena_certa.v2.preflight validate-batch --batch "${{ steps.read.outputs.batch_path }}" --expect "${{ steps.read.outputs.candidate_count }}"

  prepare-10:
    needs: dispatch
    if: needs.dispatch.outputs.enabled == 'true' && needs.dispatch.outputs.mode == 'PREPARE'
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        index: ${{ fromJSON(needs.dispatch.outputs.index_matrix) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    container:
      image: ghcr.io/umagestaointeligente/cena-certa-runtime@sha256:5e35dcb15a263b190fd91ad759c6e570d16a186c180fe18fe3f8080ac86acf9c
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v7
      - name: Deadline guard
        shell: bash
        run: |
          set -euo pipefail
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_BEFORE_PREPARE; exit 1; }
      - name: Prepare sealed candidate
        shell: bash
        run: |
          set -euo pipefail
          export PYTHONPATH="$ROOT"
          python -m factory.cena_certa.v2.prepare --batch "${{ needs.dispatch.outputs.batch_path }}" --index "${{ matrix.index }}" --prepared-root prepared
          test "$(find prepared -name prepared.json | wc -l | tr -d ' ')" = 1
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_AFTER_PREPARE; exit 1; }
      - uses: actions/upload-artifact@v4
        with:
          name: cena-certa-ready-slot-${{ matrix.index }}
          path: prepared/
          if-no-files-found: error
          retention-days: 4

  ready-proof:
    needs: [dispatch, prepare-10]
    if: always() && needs.dispatch.outputs.enabled == 'true' && needs.dispatch.outputs.mode == 'PREPARE'
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    outputs:
      ready_count: ${{ steps.proof.outputs.ready_count }}
      ready_matrix: ${{ steps.proof.outputs.ready_matrix }}
    steps:
      - uses: actions/checkout@v7
      - name: Download every successful prepared slot
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: cena-certa-ready-slot-*
          path: prepared
          merge-multiple: true
      - id: proof
        name: Prove resilient 8+2 READY queue and emit only valid render slots
        shell: bash
        run: |
          set -euo pipefail
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_BEFORE_READY_PROOF; exit 1; }
          export BATCH='${{ needs.dispatch.outputs.batch_path }}' CANDIDATE_COUNT='${{ needs.dispatch.outputs.candidate_count }}' SELECTED_COUNT='${{ needs.dispatch.outputs.selected_count }}' RESERVE_COUNT='${{ needs.dispatch.outputs.reserve_count }}'
          python - <<'PY'
          import glob,json,os
          batch=json.load(open(os.environ['BATCH'],encoding='utf-8'))
          index_by_id={x['id']:i for i,x in enumerate(batch)}
          files=glob.glob('prepared/*/prepared.json')
          count=len(files); candidate_count=int(os.environ['CANDIDATE_COUNT']); selected_count=int(os.environ['SELECTED_COUNT']); reserve_count=int(os.environ['RESERVE_COUNT'])
          if candidate_count!=selected_count+reserve_count or count<selected_count or count>candidate_count:
              raise SystemExit(f'READY_COUNT_FAIL {count} expected {selected_count}..{candidate_count}')
          ids=[]; indices=[]
          for p in files:
              d=json.load(open(p,encoding='utf-8'))
              if d.get('schema')!='CENA_CERTA_PREPARED_ASSETS_V2' or d.get('prepared_pass') is not True:
                  raise SystemExit(f'READY_MANIFEST_FAIL {p}')
              if not d.get('prepared_engine_fingerprint') or not d.get('prepared_asset_fingerprint'):
                  raise SystemExit(f'READY_FINGERPRINT_MISSING {p}')
              rid=d.get('id')
              if rid not in index_by_id:
                  raise SystemExit(f'READY_ID_NOT_IN_BATCH {rid}')
              ids.append(rid); indices.append(index_by_id[rid])
          if len(set(ids))!=count or len(set(indices))!=count:
              raise SystemExit('READY_ID_OR_INDEX_DUPLICATE')
          indices=sorted(indices)
          state='READY_FULL' if count==candidate_count else 'READY_DEGRADED'
          proof={'schema':'CENA_CERTA_READY_QUEUE_PROOF_V2','state':state,'run_id':'${{ github.run_id }}','batch_sha256':'${{ needs.dispatch.outputs.batch_sha256 }}','ready_count':count,'candidate_ids':sorted(ids),'ready_indices':indices,'minimum_required':selected_count,'max_candidate_failures':candidate_count-selected_count}
          json.dump(proof,open('ready-proof.json','w'),indent=2)
          matrix=json.dumps(indices,separators=(',',':'))
          with open(os.environ['GITHUB_OUTPUT'],'a',encoding='utf-8') as f:
              f.write(f'ready_count={count}\nready_matrix={matrix}\n')
          print('READY_QUEUE_8_PLUS_PASS count=',count,'indices=',indices,'run_id=${{ github.run_id }}')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: cena-certa-ready-proof-${{ github.run_id }}
          path: ready-proof.json
          if-no-files-found: error
          retention-days: 4

  render-stage-10:
    needs: [dispatch, ready-proof]
    if: needs.dispatch.outputs.enabled == 'true' && needs.dispatch.outputs.mode == 'PREPARE' && needs.ready-proof.result == 'success'
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        index: ${{ fromJSON(needs.ready-proof.outputs.ready_matrix) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 7
    container:
      image: ghcr.io/umagestaointeligente/cena-certa-runtime@sha256:5e35dcb15a263b190fd91ad759c6e570d16a186c180fe18fe3f8080ac86acf9c
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    env:
      URL: ${{ secrets.UGI_VIDEO_UPLOAD_URL }}
      KEY: ${{ secrets.UGI_VIDEO_UPLOAD_KEY }}
      ORBIT_X264_PRESET: veryfast
      ORBIT_CRF: '18'
      ORBIT_CONTACT_SHEET: '0'
      ORBIT_ALLOW_AUDIO_REPAIR: '0'
    steps:
      - uses: actions/checkout@v7
      - name: Download this proven-ready slot from the current canonical run
        uses: actions/download-artifact@v4
        with:
          name: cena-certa-ready-slot-${{ matrix.index }}
          path: prepared
      - id: clock
        shell: bash
        run: |
          set -euo pipefail
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_BEFORE_RENDER; exit 1; }
          echo "started=$(date +%s)" >> "$GITHUB_OUTPUT"
      - name: Offline render, QA and reconciled R2 staging
        shell: bash
        run: |
          set -euo pipefail
          test -n "$URL" || { echo R2_URL_MISSING; exit 1; }
          test -n "$KEY" || { echo R2_KEY_MISSING; exit 1; }
          export PYTHONPATH="$ROOT"
          python -m factory.cena_certa.v2.render --batch "${{ needs.dispatch.outputs.batch_path }}" --index "${{ matrix.index }}" --prepared-root prepared
          export BATCH="${{ needs.dispatch.outputs.batch_path }}" INDEX="${{ matrix.index }}"
          eval "$(python - <<'PY'
          import json,os,shlex
          item=json.load(open(os.environ['BATCH'],encoding='utf-8'))[int(os.environ['INDEX'])]
          print('RID='+shlex.quote(item['id']))
          PY
          )"
          file="$ROOT/output/cena-certa-v2/${RID}.mp4"
          receipt="$ROOT/output/cena-certa-v2/${RID}.receipt.json"
          test -s "$file"; test -s "$receipt"
          dur=$(ffprobe -v error -show_entries format=duration -of csv=p=0 "$file")
          python ops/cena-certa/r2_stage.py \
            --base-url "$URL" \
            --auth-key "$KEY" \
            --id "$RID" \
            --batch-sha256 "${{ needs.dispatch.outputs.batch_sha256 }}" \
            --mp4 "$file" \
            --duration "$dur" \
            --out "$ROOT/output/cena-certa-v2/${RID}.r2.json"
          elapsed=$(($(date +%s)-${{ steps.clock.outputs.started }}))
          echo "RENDER_STAGE_SECONDS=$elapsed"
          test "$elapsed" -le 360 || { echo RENDER_STAGE_SLA_FAIL; exit 1; }
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_AFTER_RENDER; exit 1; }
          mkdir -p proof
          cp "$receipt" proof/
          cp "$ROOT/output/cena-certa-v2/${RID}.r2.json" proof/
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cena-certa-render-slot-${{ matrix.index }}
          path: proof/
          if-no-files-found: warn
          retention-days: 4

  finalize-8-plus-2:
    needs: [dispatch, ready-proof, render-stage-10]
    if: always() && needs.dispatch.outputs.enabled == 'true' && needs.dispatch.outputs.mode == 'PREPARE' && needs.ready-proof.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    container:
      image: ghcr.io/umagestaointeligente/cena-certa-runtime@sha256:5e35dcb15a263b190fd91ad759c6e570d16a186c180fe18fe3f8080ac86acf9c
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Download every successful render proof
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: cena-certa-render-slot-*
          path: staged
          merge-multiple: true
      - name: Select eight, preserve reserves, close dispatch and create exact publisher outbox
        shell: bash
        run: |
          set -euo pipefail
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_BEFORE_FINALIZE; exit 1; }
          export PYTHONPATH="$ROOT"
          mkdir -p "$ROOT/output/cena-certa-v2"
          find staged -maxdepth 1 -type f -name '*.json' -exec cp {} "$ROOT/output/cena-certa-v2/" \; 2>/dev/null || true
          python -m factory.cena_certa.v2.aggregate --batch "${{ needs.dispatch.outputs.batch_path }}" --expect-pool "${{ needs.dispatch.outputs.candidate_count }}" --select "${{ needs.dispatch.outputs.selected_count }}" --reserves "${{ needs.dispatch.outputs.reserve_count }}"
          python -m factory.cena_certa.v2.schedule_manifest --batch "$ROOT/output/cena-certa-v2/selected-batch.json" --staged-root "$ROOT/output/cena-certa-v2" --out "$ROOT/output/cena-certa-v2/schedule-handoff.json" --expect-posts "${{ needs.dispatch.outputs.selected_count }}"
          export RUN_ID='${{ github.run_id }}' BATCH_SHA='${{ needs.dispatch.outputs.batch_sha256 }}' BATCH_PATH='${{ needs.dispatch.outputs.batch_path }}' REQUEST_ID='${{ needs.dispatch.outputs.request_id }}' SELECTED_COUNT='${{ needs.dispatch.outputs.selected_count }}' RESERVE_COUNT='${{ needs.dispatch.outputs.reserve_count }}'
          python - <<'PY'
          import datetime,hashlib,json,os,pathlib,time
          deadline=int('${{ needs.dispatch.outputs.deadline_epoch }}')
          if int(time.time())>=deadline:
              raise SystemExit('PRODUCTION_SLA_EXPIRED_DURING_FINALIZE')
          root=pathlib.Path(os.environ['ROOT'])/'output/cena-certa-v2'
          hp=root/'schedule-handoff.json'; h=hp.read_bytes(); hsha=hashlib.sha256(h).hexdigest(); hand=json.loads(h)
          selected_count=int(os.environ['SELECTED_COUNT']); reserve_count=int(os.environ['RESERVE_COUNT']); expected_placements=selected_count*4
          if hand.get('count')!=selected_count or hand.get('expectedNetworkPlacements')!=expected_placements:
              raise SystemExit('OUTBOX_HANDOFF_COUNT_FAIL')
          pool=json.loads((root/'pool-report.json').read_text(encoding='utf-8'))
          if pool.get('batch_sha256')!=os.environ['BATCH_SHA']:
              raise SystemExit('POOL_BATCH_BINDING_FAIL')
          out={'schema':'CENA_CERTA_PUBLISHER_OUTBOX_V1','state':'PREVIEW_READY_HUMAN_APPROVAL_REQUIRED','production_run_id':int(os.environ['RUN_ID']),'batch_sha256':os.environ['BATCH_SHA'],'handoff_sha256':hsha,'generated_at':datetime.datetime.now(datetime.timezone.utc).isoformat(),'selected_ids':pool['selected_ids'],'reserve_ids':pool['reserve_ids'],'expected_schedule_objects':selected_count,'expected_network_placements':expected_placements,'schedule_gate':'BLOCKED_UNTIL_PRIVATE_PREVIEW_AND_HUMAN_APPROVAL','items':hand['items']}
          pathlib.Path('/tmp/publisher-outbox.json').write_text(json.dumps(out,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          approval={'schema':'CENA_CERTA_HUMAN_APPROVAL_V1','approved':False,'handoff_sha256':hsha,'production_run_id':int(os.environ['RUN_ID']),'approved_at':None,'approved_by':None,'note':'Exact new outbox requires explicit human approval before scheduling.'}
          pathlib.Path('/tmp/human-approval.json').write_text(json.dumps(approval,indent=2)+'\n')
          state={'schema':'CENA_CERTA_PUBLISHER_STATE_V1','state':'AWAITING_HUMAN_APPROVAL','handoff_sha256':hsha,'production_run_id':int(os.environ['RUN_ID']),'primary_provider':'METRICOOL','primary_receipts':[],'secondary_receipts':[],'reconciliation':None,'updated_at':datetime.datetime.now(datetime.timezone.utc).isoformat(),'blind_retry_forbidden':True}
          pathlib.Path('/tmp/publisher-state.json').write_text(json.dumps(state,indent=2)+'\n')
          print('PUBLISHER_OUTBOX_READY',hsha,'selected',len(out['selected_ids']),'reserves',len(out['reserve_ids']))
          PY
          git config user.name 'Cena Certa Factory V2'
          git config user.email 'actions@users.noreply.github.com'
          git fetch origin "$GITHUB_REF_NAME"
          git rebase "origin/$GITHUB_REF_NAME"
          export CURRENT_DISPATCH=ops/cena-certa/dispatch.json
          python - <<'PY'
          import json,os,pathlib
          p=pathlib.Path(os.environ['CURRENT_DISPATCH'])
          d=json.loads(p.read_text(encoding='utf-8'))
          if d.get('schema')!='CENA_CERTA_PRODUCTION_DISPATCH_V1' or d.get('enabled') is not True or str(d.get('mode') or '').upper()!='PREPARE':
              raise SystemExit('DISPATCH_CHANGED_DURING_RUN')
          if str(d.get('batch_path') or '')!=os.environ['BATCH_PATH'] or str(d.get('batch_sha256') or '')!=os.environ['BATCH_SHA'] or str(d.get('request_id') or '')!=os.environ['REQUEST_ID']:
              raise SystemExit('DISPATCH_CAS_FAIL')
          idle={'schema':'CENA_CERTA_PRODUCTION_DISPATCH_V1','enabled':False,'mode':'IDLE','batch_path':'','batch_sha256':'','prepared_run_id':'','requested_at':'','request_id':'','note':'Automatically closed after one canonical single-run factory execution; human scheduling gate remains closed.'}
          pathlib.Path('/tmp/dispatch-idle.json').write_text(json.dumps(idle,indent=2)+'\n')
          PY
          cp /tmp/publisher-outbox.json ops/cena-certa/publisher-outbox.json
          cp /tmp/human-approval.json ops/cena-certa/human-approval.json
          cp /tmp/publisher-state.json ops/cena-certa/publisher-state.json
          cp /tmp/dispatch-idle.json ops/cena-certa/dispatch.json
          python ops/cena-certa/factory_doctor.py --state-only
          git add ops/cena-certa/publisher-outbox.json ops/cena-certa/human-approval.json ops/cena-certa/publisher-state.json ops/cena-certa/dispatch.json
          git commit -m "Cena Certa V2: close dispatch and publish human-gated outbox ${{ github.run_id }} [skip ci]"
          test "$(date +%s)" -lt "${{ needs.dispatch.outputs.deadline_epoch }}" || { echo PRODUCTION_SLA_EXPIRED_BEFORE_COMMIT; exit 1; }
          git push origin "HEAD:$GITHUB_REF_NAME"
      - uses: actions/upload-artifact@v4
        with:
          name: cena-certa-publisher-outbox-${{ github.run_id }}
          path: |
            vendor/cena-certa-factory-v2/src/output/cena-certa-v2/pool-report.json
            vendor/cena-certa-factory-v2/src/output/cena-certa-v2/selected-batch.json
            vendor/cena-certa-factory-v2/src/output/cena-certa-v2/hot-reserve.json
            vendor/cena-certa-factory-v2/src/output/cena-certa-v2/schedule-handoff.json
          if-no-files-found: error
          retention-days: 7
