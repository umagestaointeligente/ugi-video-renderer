UGI — AUTONOMOUS BRIDGE HANDOFF
STANDARD: LAPB-1.0
PROJECT: UGI — Uma Gestão Inteligente
DATE: 2026-08-23
STATUS: FOUNDATION_IN_PROGRESS
FAIL_CLOSED=true

1. PROJECT IDENTITY
Repository: umagestaointeligente/ugi-video-renderer
Default branch: main
Admin bridge target Worker: lola-github-admin-bridge-ugi
Admin bridge source: cloudflare/lola-github-admin-bridge-ugi.mjs
Wrangler config: cloudflare/wrangler.ugi-admin.toml
Health workflow: .github/workflows/actions-health.yml

2. ISOLATION RULE
The UGI administrative bridge MUST use a GitHub App installation scoped only to:
umagestaointeligente/ugi-video-renderer

The currently connected ChatGPT GitHub installation is NOT accepted as the UGI administrative bridge because its installation can access multiple project repositories, including Bom de Clique, Orbit Media Labs and BFY. It may remain a conversation connector, but it is not the project-isolated production bridge.

Never use All repositories for the UGI bridge.
Never reuse another project's private key or BRIDGE_AUTH_SECRET.
Never allow owner/repo to be supplied by request.

3. REQUIRED GITHUB APP
Preferred name: Lola Operations Bridge — UGI
Installation scope: Only select repositories -> umagestaointeligente/ugi-video-renderer only
OAuth user authorization: off
Device flow: off
Webhook: off unless later justified

Recommended repository permissions:
Metadata: Read-only
Actions: Read and write
Administration: Read and write
Contents: Read and write
Issues: Read and write
Checks: Read and write
Commit statuses: Read and write
Pull requests: Read and write

Optional only if later required:
Actions variables, Deployments, Environments

4. REQUIRED CLOUDFLARE WORKER SECRETS — NAMES ONLY
GITHUB_APP_ID
GITHUB_INSTALLATION_ID
GITHUB_APP_PRIVATE_KEY
BRIDGE_AUTH_SECRET

No secret values belong in GitHub source or this handoff.
After any Worker/App/schema/binding/credential change, re-save/revalidate all four credentials before declaring READY.

5. WORKER HARD LOCK
OWNER=umagestaointeligente
REPO=ugi-video-renderer
DEFAULT_BRANCH=main

Current workflow allowlist in bridge source:
actions-health.yml
ugi-growth-policy-smoke.yml
render-video.yml
deploy-cloudflare-worker.yml

6. ADMIN ENDPOINTS
Public:
GET /health

Authenticated read-only:
GET /diagnostics
GET /github/actions/status
GET /github/workflows
GET /github/workflows/runs
GET /github/workflows/jobs?run_id=<id>
GET /github/pipeline/health

Authenticated controlled write:
POST /github/actions/enable
POST /github/workflows/dispatch
POST /github/recovery/repair

All authenticated routes require BRIDGE_AUTH_SECRET bearer auth.
The Worker rejects non-allowlisted workflows and refs other than main.

7. STABLE UGI COMPONENTS TO PRESERVE
Existing Lola Operacional UGI Worker and its live production routes
UGI Store
Commerce / checkout / fulfillment
R2 storage and receipts
Buffer as exclusive UGI publisher
Metricool analytics-only role
GitHub Actions renderer pipeline
Semantic validation
Copy lock
Publication gates
Growth Policy runtime loader and smoke evidence
Control Plane documentation
Existing credentials and integrations that are already stable

Do not replace the live Lola Operacional Worker with an older repository source.
The new admin bridge is a separate Worker and must not share production secrets with the Lola Operacional Worker unless explicitly designed later.

8. EXISTING GROWTH POLICY EVIDENCE
Policy source: config/ugi/growth-policy.json
Policy id: ugi-growth-engine
Policy schema observed: 1.2
Runtime smoke previously passed.
Known receipt: control-plane/receipts/ugi-growth-engine/latest.json
Known run id from prior validation: 32659766889
Publication triggered during smoke: false
Payment triggered during smoke: false

9. ACTIONS HEALTH
Workflow: .github/workflows/actions-health.yml
Required properties:
workflow_dispatch: explicit
push on main for bridge/health/handoff changes
job always starts
no job-level route gate
runner execution step
bridge source syntax check
repository lock source check
workflow allowlist source check
compact receipt
artifact evidence
persistent receipt commit with [skip ci]

READY requires:
WORKFLOW_RUN_CREATED=true
TOTAL_JOBS>0
NO_JOBS_WERE_RUN=false
job conclusion=success

10. CURRENT HUMAN GATE
A project-specific GitHub App suitable for UGI administrative production use has not yet been proven.
The currently connected installation spans multiple project repositories and therefore cannot satisfy the LAPB isolation requirement for this bridge.

First human-required action when reached:
Create or install a dedicated GitHub App named approximately 'Lola Operations Bridge — UGI' and scope its installation to ONLY umagestaointeligente/ugi-video-renderer. Generate the App private key securely without pasting it into ChatGPT.

After that, operations resume autonomously from:
- capture App ID and installation ID without exposing secrets;
- configure Cloudflare Worker secrets in secure provider storage;
- deploy lola-github-admin-bridge-ugi;
- revalidate all four credential bindings;
- GET /health;
- GET /diagnostics;
- GET /github/actions/status;
- dispatch actions-health.yml;
- read run;
- read jobs;
- require total_jobs>0 and success;
- persist evidence;
- update this handoff;
- only then set AUTONOMY_FOUNDATION_READY=true.

11. SUCCESS DEFINITION
PROJECT_ISOLATED=true
GITHUB_APP_AUTH_OK=true
INSTALLATION_AUTH_OK=true
REPOSITORY_LOCK_OK=true
ACTIONS_ENABLED=true
HEALTH_WORKFLOW_DISPATCHED=true
WORKFLOW_RUN_CREATED=true
TOTAL_JOBS>0
NO_JOBS_WERE_RUN=false
MOBILE_OPERATION_PATH_AVAILABLE=true
SECRETS_EXPOSED=false
FAIL_CLOSED=true

Until all gates pass:
AUTONOMY_FOUNDATION_READY=false
