# LSI — RECOVERY POINTER

Status: CANÔNICO / LOCALIZADOR ESTÁVEL

Âncora humana oficial de recuperação entre chats:

`Recovery LSI`

Alias técnico interno de compatibilidade:

`LSI::RECOVERY::CURRENT`

## Como recuperar

1. Ao receber `Recovery LSI`, tratar como comando completo de recuperação canônica do ecossistema LSI.
2. Ler este ponteiro no `main`.
3. Ler no próprio `main`, nesta ordem:
   - `docs/LSI_CANONICAL_INDEX.md`
   - `docs/LSI_RECOVERY_CURRENT.md`
   - manifesto indicado por `CURRENT_FOCUS` (atualmente `docs/projects/LSI_CAREER360.md`).
4. Ler documentos especializados somente quando o manifesto indicar necessidade.
5. Quando estado operacional vivo importar, fazer readback da fonte/runtime antes de declarar PASS/READY/LIVE.

Resposta esperada do chat novo na primeira linha:

`LSI_RECOVERY=TRUE`

## Regra

O usuário não precisa memorizar nem digitar sintaxe técnica. Para ele, basta:

`Recovery LSI`

O alias técnico existe apenas para compatibilidade interna, automações e documentação de sistema.

Este arquivo é apenas um LOCALIZADOR. Não duplicar o snapshot CURRENT aqui.
O Career 360 Master Pilot 1.0 já foi promovido para `main`; não procurar a antiga branch de construção como fonte CURRENT.
