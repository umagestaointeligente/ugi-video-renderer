{
  "schema_version": "1.0",
  "effective_date": "2026-08-23",
  "project": "UGI",
  "routing_policy": {
    "publishing": {
      "provider": "buffer",
      "exclusive": true,
      "allowed_operations": ["schedule","queue","publish","retry","publication_status","publication_readback"],
      "fallback_provider": null,
      "fail_closed_if_unavailable": true
    },
    "analytics": {
      "provider": "metricool",
      "read_only": true,
      "allowed_operations": ["analytics_read","best_time_read","performance_read","historical_analysis"],
      "forbidden_operations": ["create_post","update_post","schedule_post","reschedule_post","queue_post","publish_post","retry_publication"]
    },
    "native_platforms": {
      "role": "verification_and_supplemental_analytics",
      "publishing_default": false
    }
  },
  "hard_invariants": [
    "Metricool must never mutate the UGI publishing plan.",
    "Metricool must never be used as fallback publisher.",
    "Buffer is the sole official scheduler and publisher for UGI.",
    "If Buffer publication cannot be proven by readback, status is BLOCKED or NOT_PROVEN.",
    "Any migration away from Buffer requires an explicit ADR, smoke test, rollback plan and policy update before production use."
  ]
}